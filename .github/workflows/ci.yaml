name: ci

on:
        push:
        pull_request:
jobs:
        main:
                runs-on: ubuntu-20.04
                steps:
                -
                        name: checkout
                        uses: actions/checkout@v2
                -
                        name: run
                        run: sudo -E make test;
